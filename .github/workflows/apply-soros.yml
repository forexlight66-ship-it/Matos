name: Soros patch workflow disabled
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary Soros patch workflow disabled; Soros engine is already implemented in hooks/useDeriv.ts."